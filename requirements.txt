# Web framework
flask>=3.0.0
flask-cors>=4.0.0
waitress>=3.0.0

# Cheminformatics
rdkit>=2023.9.1
openbabel-wheel>=3.1.1
py3Dmol>=2.0.3

# Numerics / graph
numpy>=1.26.0
scipy>=1.12.0
networkx>=3.3

# Molecular simulation
ase>=3.23.0
dscribe>=2.1.0

# TorchANI — neural network potentials (requires torch, installed in Dockerfile)
torchani>=2.2.4

# AI conformer generation
# auto3d is installed with --no-deps in the Dockerfile to avoid re-pulling CUDA torch
# auto3d==2.3.3

# GLOMOS — genetic algorithm for rotamer search
# Installed in Dockerfile (pip install glomos or from local /app/glomos)
# glomos
